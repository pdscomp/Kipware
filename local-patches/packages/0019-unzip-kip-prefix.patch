--- a/utils/unzip/Makefile	2026-05-16 19:02:47.938017122 -0400
+++ b/utils/unzip/Makefile	2026-05-16 19:02:47.930267387 -0400
@@ -34,7 +34,7 @@
   TITLE:=De-archiver for .zip files
   URL:=http://infozip.sourceforge.net/UnZip.html
   SUBMENU:=Compression
-  ALTERNATIVES:=300:/opt/bin/unzip:/opt/libexec/unzip
+  ALTERNATIVES:=300:/kip/bin/unzip:/kip/libexec/unzip
 endef
 
 define Package/unzip/description
@@ -67,13 +67,13 @@
 endef
 
 define Package/unzip/install
-	$(INSTALL_DIR) $(1)/opt/bin/
+	$(INSTALL_DIR) $(1)/kip/bin/
 	$(INSTALL_BIN) \
-		$(PKG_INSTALL_DIR)/opt/bin/{funzip,unzipsfx,zipgrep} \
-		$(1)/opt/bin/
-	$(INSTALL_DIR) $(1)/opt/libexec
-	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/opt/bin/unzip $(1)/opt/libexec
-	$(LN) unzip $(1)/opt/bin/zipinfo
+		$(PKG_INSTALL_DIR)/kip/bin/{funzip,unzipsfx,zipgrep} \
+		$(1)/kip/bin/
+	$(INSTALL_DIR) $(1)/kip/libexec
+	$(INSTALL_BIN) $(PKG_INSTALL_DIR)/kip/bin/unzip $(1)/kip/libexec
+	$(LN) unzip $(1)/kip/bin/zipinfo
 endef
 
 $(eval $(call BuildPackage,unzip))
